import { FC } from 'react';

interface CardMenuProps {
  title: string;
  items: string;
  frames: string[];
  price: number;
  quantity: number;
  calorie: number;
}

const CardMenu: FC<CardMenuProps> = () => <></>;

export default CardMenu;
